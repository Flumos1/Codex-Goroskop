{
  "name": "codex-goroskop-workers",
  "private": true,
  "description": "Cloudflare Workers deployment for Codex Goroskop",
  "scripts": {
    "build": "node scripts/build-assets.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
